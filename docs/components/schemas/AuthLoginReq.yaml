  type: object
  properties:
    email:
      type: string
      format: email
      example: gordon@example.com
    password:
      type: string
      format: password
      example: mypassword123
  required:
    - email
    - password
